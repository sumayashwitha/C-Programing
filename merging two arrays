#include<stdio.h>
int main(){
   int a[50], b[50], merged[100];
   int i, k, n1,n2;
   printf("enter the size of furst array:\n");
   scanf("%d", &n1);
   printf("the elements in the first array:\n");
   for(i=0;i<n1;i++){
       scanf("%d", &a[i]);
  }
  printf("enter the size of second array:\n");
  scanf("%d", &n2);
  printf("enter the elements in the second array:\n");
  for(i=0;i<n2;i++){
       scanf("%d", &b[i]);
  }
  k=0;
  for(i=0;i<n1;i++) merged[k++] = a[i];
  for(i=0;i<n2;i++) merged[k++] = b[i];

  printf("the merged array\n");
  for(i=0;i<k;i++){
  printf(" %d", merged[i]);
  }
  print("\n");
  return 0;
  }

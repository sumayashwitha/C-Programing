#include<stdio.h>
int main(void){
    int num, sum=0, digit;
    printf("enter the number:");
    scanf("%d",&num);
    while(num>0){
          digit=num%10;
          sum= sum+(digit);
          num=num/10;
    }
    printf("%d is the sum of the digits of the number", sum);
   return 0;
}
